# Project progress from immutable checkpoints

Runner work and direct project work will enter Gareji Board through one immutable Progress Checkpoint ledger, with Board activity and knowledge-workspace notes derived as projections. This chooses visible eventual consistency over bidirectional task synchronization, preserves Board ownership of Work item state, and prevents an unavailable documentation repository from losing or blocking local progress.
